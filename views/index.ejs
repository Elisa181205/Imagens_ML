<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sua Página</title>
</head>
<body>
  <h1>Lista Suspensa de Resultados da API</h1>
  <select id="dropdown">
    <% results.forEach(function(result) { %>
      <option value="<%= result %>"><%= result %></option>
    <% }); %>
  </select>
</body>
</html>
